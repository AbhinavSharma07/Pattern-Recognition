pydantic>=2.0
python-dotenv>=1.0
typer>=0.12
langchain-core>=0.2
langchain-openai>=0.1
langchain-community>=0.2
langgraph>=0.1
gradio>=4.44
spaces>=0.30
libcst>=1.1
pytest>=7.4

# Optional: only needed if you enable the Docker sandbox (--docker / "Use Docker Sandbox")
docker>=7.0
